services:
  redis-database:
    image: redis
    restart: always
    container_name: mailstorage-redis
    command: >
      --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"

volumes:
  redis_data:
    external: false
